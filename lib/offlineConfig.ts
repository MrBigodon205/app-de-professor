// Feature flag to control Offline Mode
// Set to true for PC App / PWA builds
// Set to false for standard Web App builds
export const ENABLE_OFFLINE_MODE = false;

console.log(`[App Config] Offline Mode is: ${ENABLE_OFFLINE_MODE ? 'ENABLED' : 'DISABLED'}`);
